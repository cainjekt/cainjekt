apiVersion: v1
kind: Pod
metadata:
  name: wget-enabled
  namespace: cainjekt-demo
  labels:
    cainjekt.io/enabled: "true"
spec:
  restartPolicy: Never
  containers:
  - name: client
    image: alpine:3.20
    command: ["sh", "-lc", "sleep 3600"]

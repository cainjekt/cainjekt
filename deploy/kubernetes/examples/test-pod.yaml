apiVersion: v1
kind: Pod
metadata:
  name: test-ca-injection
  annotations:
    cainjekt.tsuzu.dev/enabled: "true"
spec:
  containers:
  - name: ubuntu
    image: ubuntu:22.04
    command: ["sleep", "infinity"]
  restartPolicy: Never
